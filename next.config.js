/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['images.unsplash.com','www.annonce-algerie.com'],
  },
}

module.exports = nextConfig
